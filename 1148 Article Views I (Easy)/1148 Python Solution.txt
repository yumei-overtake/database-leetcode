import pandas as pd

def article_views(views: pd.DataFrame) -> pd.DataFrame:
    return views[views['author_id']==views['viewer_id']]['author_id']\
          .drop_duplicates(keep='first')\
          .sort_values(ascending=True)\
          .to_frame(name='id')



    # use \ at the end of line can make code cleaner