import pandas as pd

def confirmation_rate(signups: pd.DataFrame, confirmations: pd.DataFrame) -> pd.DataFrame:
    df = signups.merge(confirmations,on='user_id',how='left')
    df['confirmed']= np.where(df['action']=='confirmed',1,0)
    return df.groupby('user_id')['confirmed'].mean().round(2).fillna(0).reset_index().rename(columns={'confirmed':'confirmation_rate'})