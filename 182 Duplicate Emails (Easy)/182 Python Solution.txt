import pandas as pd

def duplicate_emails(person: pd.DataFrame) -> pd.DataFrame:
    val= person[person['email'].duplicated(keep=False)]['email'].unique()
    return pd.DataFrame(val,columns=['Email'])



# notes: person['email'].duplicated(keep=False) work as data mask, return a series of Boolean