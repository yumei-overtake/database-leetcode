import pandas as pd

def find_employees(employees: pd.DataFrame) -> pd.DataFrame:
    return employees[(~employees['manager_id'].isin(employees['employee_id']))&(employees['salary']<30000) & (~employees['manager_id'].isnull())][['employee_id']].sort_values(by='employee_id')