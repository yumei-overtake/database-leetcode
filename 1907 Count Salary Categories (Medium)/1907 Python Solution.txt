import pandas as pd

def count_salary_categories(accounts: pd.DataFrame) -> pd.DataFrame:
    df = pd.DataFrame({'category':['Low Salary','Average Salary','High Salary']})
    accounts['category']= np.where(  accounts['income']<20000,'Low Salary',np.where (accounts['income']>50000,'High Salary','Average Salary'))
    df_acc = accounts.groupby('category')['account_id'].size().reset_index(name='accounts_count')
    return df.merge(df_acc , on ='category',how='left').fillna(0)