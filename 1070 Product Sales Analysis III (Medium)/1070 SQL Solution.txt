with a as (
SELECT product_id,
       min(year) as first_year
FROM sales
GROUP BY product_id
)
select s.product_id,
       s.year as first_year,
       quantity ,
       price
from sales as s
inner join a on a.product_id=s.product_id
            and a.first_year=s.year